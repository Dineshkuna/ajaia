# Only needed for production builds where frontend and backend are deployed
# separately. Leave unset for local dev — Vite proxies /api to localhost:4000.
VITE_API_BASE_URL=https://your-backend-host.example.com
